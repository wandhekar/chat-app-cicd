apiVersion: v1
kind: ConfigMap
metadata:
  name: chat-app-config
  labels:
    app: chat-app
data:
  # Ollama configuration - Using actual Windows host IP
  OLLAMA_HOST: "192.168.2.11"
  OLLAMA_PORT: "11434"
  
  # App configuration
  APP_NAME: "Chat App CI/CD Demo"
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "development"